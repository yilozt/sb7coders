{
  "scripts": {
    "build": "rm -rf ./pkg && rm -rf ./dist/* && wasm-pack build --release -d pkg && webpack",
    "serve": "webpack serve --host 0.0.0.0"
  },
  "devDependencies": {
    "html-webpack-plugin": "^5.5.0",
    "text-encoding": "^0.7.0",
    "webpack": "^5.69.1",
    "webpack-cli": "^4.9.2",
    "webpack-dev-server": "^4.7.4",
    "webpack-watch-files-plugin": "^1.2.0"
  }
}
